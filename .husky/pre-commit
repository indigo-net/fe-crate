echo ""
echo "⚠️  커밋 전 FSD 레이어 검증이 필요합니다."
echo ""
echo "   AI 에이전트를 활용 중이라면 이렇게 요청해보세요:"
echo "   \"@project-agents/fsd-reviewer 로 변경 파일 검증해줘\""
echo ""

exec < /dev/tty
read -r -p "FSD 검증을 완료했습니까? (y/n): " answer

if [ "$answer" != "y" ]; then
  echo "커밋이 중단되었습니다. 검증 후 다시 시도해주세요."
  exit 1
fi
